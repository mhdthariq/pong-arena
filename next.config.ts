import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Removed static export since we're using dynamic routes with Firebase
  // output: "export",
};

export default nextConfig;
